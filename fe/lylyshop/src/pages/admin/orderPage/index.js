import { memo } from "react";
import "./style.scss";
import { useNavigate } from "react-router-dom";
import { ROUTERS } from "utils/router";

function OrderPage() {
  

  return (
    <div className="container">OrderPage</div>
  );
}

export default memo(OrderPage);
